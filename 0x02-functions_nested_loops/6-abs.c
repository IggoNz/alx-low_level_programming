#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: input a number as an integer
 * Return: the absolute value
 */
int _abs(int i)
{
	if (i >= 1)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
